# ~/.tmuxinator/tutor.yml

name: tutor
root: ~/projects/2mundos/tutor-opentok
startup_window: editor

windows:
  - server:
      layout: even-horizontal
      panes:
        - meteor --settings settings.json
        - meteor test --driver-package practicalmeteor:mocha --port 3002
  - editor: nvim
  - terminal: wat
